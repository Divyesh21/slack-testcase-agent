# ─────────────────────────────────────────────────────────────────────────────
# Lambda / container image dependencies.
# These are installed inside the Docker image (see Dockerfile).
# Pin major versions so builds are reproducible.
# ─────────────────────────────────────────────────────────────────────────────

# Slack
slack_bolt>=1.21,<2

# OpenAI
openai>=1.30,<2

# Chroma (vector store)
chromadb>=0.5,<2

# HTTP
requests>=2.31,<3

# AWS SDK — used by slack_bolt lazy listeners for Lambda self-invocation
boto3>=1.34,<2

# Env helper (used in local_lambda_test.py; no-op in Lambda)
python-dotenv>=1.0,<2
